# Logo — kept local only. Used as source art for the GitHub repo's
# Social Preview image (Settings → General → Social preview), uploaded
# manually. Not tracked, not pushed, never rendered inline in the README.
logo.png

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
